= Changelog

All notable changes to this project will be documented in this file.

The format is based on https://keepachangelog.com/en/1.1.0/[Keep a Changelog],
and this project adheres to https://semver.org/spec/v2.0.0.html[Semantic Versioning].

== Version identity (dual-axis)

Public labels use **`VALENTUS_SEMVER+antora.N`** (example: `2.0.0+antora.3`).

* *Valentus semver* (`MAJOR.MINOR.PATCH`) — chrome/partial/CSS contract for theme modders. Lives in `package.json` and git tags `vX.Y.Z`.
* *`+antora.N`* — Antora *framework* major this release targets. Does **not** bump Valentus major by itself.
* Git tags stay pure semver (`v2.0.0`). Release titles and this changelog use the full label.
* Historical **1.x** releases were all Antora 3 → documented as `1.x.y+antora.3`. Existing tags (`v1.0.0`, …) were **not** renamed.

== [Unreleased]

* *Fix / UX:* Restore icon-only site-home chip (SVG house); keep copyable `/` trail separators. Version kicker button shows **Version** for `~`/default; dropdown rows show literal `~`. See link:changelog-details/2026-09-10%20-%20home%20chip%20and%20version%20button.adoc[Detailed notes].
* *Docs / SEO:* Antora `site.robots` now emits `/robots.txt` with an absolute `Sitemap:` line for the production host.
* *Fix:* Docs example Themed SVG host mode — ship `site-themed-svg.css` with light+dark `--themed-svg-valentus-example-*` host vars (match diagram manifests) so dark mode no longer falls back to light after runtime upgrade. See link:changelog-details/2026-09-10%20-%20themed%20svg%20host%20css.adoc[Detailed notes].
+
Debt: Mermaid host SVGs still carry a few hard-coded `fill:#…!important` rules (arrow/root anchors); regenerate with `@dev-centr/themed-svg@0.2.2` when practical.

== [2.0.30] - 2026-09-10

Public label: *2.0.30+antora.3* (Antora 3).

* *Fix / UX:* Drop Antora’s extra `.75rem` left margin on the **top-level** tree `nav > .nav-list` (rail padding already insets); keep nested `.nav-list` margins for depth indentation.

== [2.0.29] - 2026-09-10

Public label: *2.0.29+antora.3* (Antora 3).

* *Fix / UX:* page-context lead — outer `.page-context-lead` is seamless; byline sits above an inner `.page-context-panel` that holds only the metadata table (pairs with page-context **0.6.1**).
* *Fix / UX:* Source row — VCS mark on the **Source** key; value cell is text-only `View | Edit` (pairs with page-edit **0.2.1**).

== [2.0.28] - 2026-09-10

Public label: *2.0.28+antora.3* (Antora 3).

* *Fix / UX:* Breadcrumb mast — site home is a real `/` character (no chip); separators are real `/` text nodes so a drag-select copies as a pasteable path; home lights up on hover. See link:changelog-details/2026-09-10%20-%20copyable%20breadcrumb%20path.adoc[Detailed notes].

== [2.0.27] - 2026-09-10

Public label: *2.0.27+antora.3* (Antora 3).

* *Fix / UX:* Breadcrumb mast — keep the **component** kicker with site-nav-tree (mobile jump target); conjoin a literal `~` version menu to its right; drop only the trail’s duplicate component-root crumb. See link:changelog-details/2026-09-10%20-%20breadcrumb%20component%20and%20tilde%20version.adoc[Detailed notes].
* *Fix / UX:* Style page-edit **View | Edit** as a muted lead-table Source cell (`.page-context-source-actions`); keep legacy `.page-context-actions` aside selectors for hubs without a lead table.
* *Fix / UX:* Breadcrumb mast — trail segments (links, folder nodes, current page) use accent color + underline; Version/component kickers keep accent chip-on-hover but no resting underline. See link:changelog-details/2026-09-10%20-%20breadcrumb%20trail%20underline.adoc[Detailed notes].
* *Polish / UX:* Read-width control icons use sharp rectangles (drop SVG `rx`) so column glyphs contrast the soft selector bar. See link:changelog-details/2026-09-10%20-%20read-width%20sharp%20icons.adoc[Detailed notes].
* *Chore:* Bump demo playbook dep `@antora-supplemental/antora-search-chat` to `v0.1.4` (hierarchical scope tree; closed face without *Component*/*Version* kind prefix). Tag consumer topic `antora-search-chat` for post-release discovery.
* *Docs:* Customization — mast keeps component + conjoined `~` menu; trail dedupes component root under site-nav-tree.
* *Diagrams:* Replace the duplicated authored PlantUML sequence with canonical Mermaid, extract both extension examples into named sources, and ship deterministic Themed SVG adaptive/host artifacts. The UI progressively upgrades `.themed-svg` image roles with the sanitized 0.1.1 runtime. See link:changelog-details/2026-09-08%20-%20themed%20diagram%20artifacts.adoc[Detailed notes — themed diagram artifacts].

== [2.0.26] - 2026-09-10

Public label: *2.0.26+antora.3* (Antora 3).

* *Fix / UX:* Hide tree-nav scrollbars (`scrollbar-width: none` + WebKit) while keeping overflow scroll and edge fades as the horizontal affordance.

== [2.0.25] - 2026-09-10

Public label: *2.0.25+antora.3* (Antora 3).

* *Fix / UX:* Dark mode side-rail panel wash is opt-in (`html.adt-side-panels`), matching light — default `--adt-side-panel-bg` stays transparent so nav/TOC blend with the canvas until hubs enable the wash.

== [2.0.24] - 2026-09-10

Public label: *2.0.24+antora.3* (Antora 3).

* *Fix:* Tree-nav panel no longer shrink-to-fits past the fixed `--adt-nav-width` column — `.nav-panel-menu` / `nav.nav-menu` stay `width: 100%` with `min-width: 0`, so long nowrap labels scroll inside the column and edge fades can engage. See link:changelog-details/2026-09-10%20-%20nav%20panel%20width%20containment.adoc[Detailed notes].

== [2.0.23] - 2026-09-09

Public label: *2.0.23+antora.3* (Antora 3).

* *Fix / UX:* Edit leaves the breadcrumb; `@antora-supplemental/page-edit` bakes Edit + provider SVG into the page-context lead. Header VCS `<img>` uses `adt-vcs-icon-id` at generate time. Drop `site-vcs-icons.js` from footer scripts.

== [2.0.22] - 2026-09-09

Public label: *2.0.22+antora.3* (Antora 3).

* *Fix / UX:* `.page-context` bridge — aside is the panel; keys left-aligned with horizontal cell padding; footer margin top/bottom; table rows only (no double frame). Pairs with `@antora-supplemental/page-context` **0.3.3**.

== [2.0.21] - 2026-09-09

Public label: *2.0.21+antora.3* (Antora 3).

* *Fix / UX:* Tree-nav horizontal scroll — long/nowrap labels expand the scrollport; left/right edge fades toggle with overflow (`adt-nav-overflow-start` / `adt-nav-overflow-end` via `site-visual.js`) so each fade shows only while that side still overflows; mask soft-clips highlight + text together. See link:changelog-details/2026-09-09%20-%20nav%20scroll%20edge%20fades.adoc[Detailed notes].

== [2.0.20] - 2026-09-09

Public label: *2.0.20+antora.3* (Antora 3).

* *Fix / UX:* Side-rail panel wash paints the content-sized nav menu (padding + 2px radius), not the full-height `.nav` column — fixes oversized dark panel; light still defaults clean, `adt-side-panels` enables subtle wash.
* *Fix / UX:* `.page-context-table` bridge — light-mode subtle surface + thin outer/inner borders; muted smaller type; explicit no-zebra overrides against dark-mode `table tbody tr:nth-child(even)`. See link:changelog-details/2026-09-09%20-%20page-context%20table%20side%20panels.adoc[Detailed notes].

== [2.0.19] - 2026-09-09

Public label: *2.0.19+antora.3* (Antora 3).

* *Fix / UX:* Mast version control — hide when only the implicit/unversioned (`~`) version; plain underlined text for a single named version; dropdown only when multiple versions (`adt-version-mode` helper).
* *Fix / UX:* Mast kickers (version / component) rest as underlined text links; chip fill appears on hover / open.
* *Fix / UX:* Nav current-page soft-fill — `overflow: hidden` on tree labels so nowrap text cannot paint past the highlight box; tree-edge fade mask (`--adt-nav-scroll-fade`).
* *Feature:* Optional light side-rail panel wash — `html.adt-side-panels` / `data-adt-side-panels="true"` sets `--adt-side-panel-bg` (dark already defaults to elevated). Sidebar TOC CONTENTS gets padding + 2px radius.
* *Feature:* In-doc Asciidoctor `:toc:` (`.doc .toc` / `#toc`) muted smaller own block — **not** part of `@antora-supplemental/page-context` metadata. Theme also bridges `.page-context` tokens.
* *Docs:* Component picker already hidden when `site.keys.site_nav_tree=true` (2.0.14); reaffirm with site-nav-tree README.

== [2.0.18] - 2026-09-01

Public label: *2.0.18+antora.3* (Antora 3).

* *Fix:* Page title mast vertical scrollbar — 2.0.17’s `overflow-x: hidden; overflow-y: visible` on `.adt-page-title`, breadcrumb trail, and kickers forced `overflow-y` to compute as `auto` (CSS overflow pairing rule), showing a vscroll bar when descenders extended past the line box. Restore single-axis `overflow: hidden` with raised `line-height` and small `padding-bottom` so descenders stay visible without scrollbars. See link:changelog-details/2026-09-01%20-%20page%20title%20mast%20scrollbar.adoc[Detailed notes — page title mast scrollbar].

== [2.0.17] - 2026-09-01

Public label: *2.0.17+antora.3* (Antora 3).

* *Fix:* Page title, breadcrumb trail, and mast kicker labels — `overflow: hidden` with tight `line-height` (1.2) clipped lowercase descenders (g, p, y) on display fonts (Syne, Plus Jakarta Sans, etc.). Use horizontal-only ellipsis clipping, raise line-height, and keep `-webkit-line-clamp` line-height at 1.35 on narrow viewports. See link:changelog-details/2026-09-01%20-%20descender%20clip%20page%20title.adoc[Detailed notes — descender clip on page title].

== [2.0.16] - 2026-09-01

Public label: *2.0.16+antora.3* (Antora 3).

* *Fix:* Condensed (mobile) nav drawer backdrop — override `--adt-nav-bg` on `:root` / `html.dark-theme` (not bare `html`) so the token beats transparent defaults; paint nav shell layers with `var(--adt-elevated)` directly. Fixes sidebar text showing over page content in light and dark mode. See link:changelog-details/2026-09-01%20-%20mobile%20nav%20backdrop%20specificity.adoc[Detailed notes — mobile nav backdrop specificity].

== [2.0.15] - 2026-09-01

Public label: *2.0.15+antora.3* (Antora 3).

* *Feature:* Nav-tree expand/collapse chevrons (`.nav-item-toggle`) use themeable tokens `--adt-nav-item-toggle` and `--adt-nav-item-toggle-hover` (default to `--adt-ink-muted` / `--adt-ink`). Legacy dark-mode caret in `site-extra.css` follows the same token so site overlays can recolor component-level arrows without editing Valentus CSS.

== [2.0.14] - 2026-09-01

Public label: *2.0.14+antora.3* (Antora 3).

* *Fix:* Breadcrumb home icon — link to site start page (`relativize site.homeUrl`), not the active component index.
* *Fix:* Breadcrumb mast — hide component kicker when `@antora-supplemental/site-nav-tree` is active (`site.keys.site_nav_tree`); version selector and trail unchanged.
* *Fix:* Version kicker label — implicit/default version shows **Version** on the button and **Default** in the dropdown (`adt-version-label` helper); named versions unchanged.
* *Fix:* Component/version breadcrumb dropdowns — raise `.adt-doc-mast` stacking above `.adt-tool-band` so the first menu rows receive clicks instead of the read-width band below.

== [2.0.13] - 2026-08-20

Public label: *2.0.13+antora.3* (Antora 3).

* *Fix (interim hack):* FOUC / layout-settle on refresh — paint-hold (`html.adt-fouc-pending`) in `head-meta.hbs` until layered supplemental CSS loads, preference attrs apply, **and** Valentus web fonts are ready (`document.fonts.load` + `fonts.ready`, 3s timeout). Stops fallback→webfont FOUT that jumped content width. Stylesheet links stay before FOUC scripts. Pending upstream Default UI single-pass CSS / !207 / !213. See link:changelog-details/2026-08-12%20-%20fouc%20paint-hold%20hack.adoc[CSS cascade hold] and link:changelog-details/2026-08-20%20-%20font%20fouc%20paint-hold.adoc[font FOUT hold].
* *Docs / convention:* GitHub *product dependency topics* — playbook repos that pin Valentus must add topic `valentus-theme` for rediscovery (`topic:valentus-theme`). Guide xref:guide:github-topics.adoc[GitHub Topics]; `antora-registry-topics.json5` lists `productTopic` + `knownDependents`.
* *Docs:* xref:guide:value-proposition.adoc[Value Proposition & Upstream Fixes] — add Default UI MRs/branches for nav label alignment (!210), sticky footer (!211), TOC heading rail inset, and dark-mode proposal (!207) with “ship via Valentus / antora-dark-mode today” guidance; FOUC / supplemental CSS cascade row.
* *Docs:* Installation migrate-from-`antora-dark-theme` notes canonical org repos and points at the dark-mode legacy-names guide; Getting Started / README link the org Theme products map.

== [2.0.12] - 2026-08-11

Public label: *2.0.12+antora.3* (Antora 3).

* *Feature:* KaTeX math plumbing always on — `site-math.js` / `site-math.css` load KaTeX + auto-render for AsciiDoc `stem: latexmath` (`\(` `\)` / `\[` `\]`) and `$…$` / `$$…$$`. Enable `stem: latexmath` in the playbook even when a page has no formulas yet.

== [2.0.11] - 2026-08-08

Public label: *2.0.11+antora.3* (Antora 3).

* *Chore:* Alias `--adt-admonition-*` to Default UI `--tip-color` / `--note-color` / etc. so one site override recolors TIP pill and card.

== [2.0.10] - 2026-08-08

Public label: *2.0.10+antora.3* (Antora 3).

* *Fix / chore:* Sync antora-dark-mode **1.4.5** (single `background` for admonition card tints).

== [2.0.9] - 2026-08-08

Public label: *2.0.9+antora.3* (Antora 3).

* *Fix:* Admonition TIP/NOTE pills use `--adt-admonition-*` (same tokens as card border/tint) so one override recolors both; stops Default UI `#41af46` pill vs divergent card green. Sync antora-dark-mode **1.4.4**. See link:changelog-details/2026-08-08%20-%20admonition%20pill%20and%20card%20share%20tokens.adoc[Detailed notes].

== [2.0.8] - 2026-08-08

Public label: *2.0.8+antora.3* (Antora 3).

* *Fix / chore:* Sync antora-dark-mode **1.4.3** (admonition card wrap, tip green tint, inline ``td``/``dt``/``dd`` code chrome). Playbook pins updated to `v1.4.3`.
* *Fix:* Admonitions — bordered card wraps TIP/NOTE label + body (type tint on `table`). Corrects 2.0.6 content-only card that left TIP outside the border. See link:changelog-details/2026-08-08%20-%20admonition%20card%20wraps%20tip%20label.adoc[Detailed notes].
* *Fix:* TIP admonition accent restored to Antora default green `#41af46` (`--adt-admonition-tip`), not emerald/teal `#059669`. See link:changelog-details/2026-08-08%20-%20tip%20admonition%20green.adoc[Detailed notes].
* *Fix:* Inline ``code`` in admonition ``td.content`` (often bare, no ``<p>``) gets the same border/radius chrome as ``.doc p code``.

== [2.0.7] - 2026-08-08

Public label: *2.0.7+antora.3* (Antora 3).

* *Fix:* Theme toggle timing — sync antora-dark-mode **1.4.2** VT suppress (`html.adt-theme-vt` / `:active-view-transition` → `transition: none`) so Valentus `--adt-transition` (0.2s) chrome no longer finishes ahead of the 0.28s root crossfade. See link:changelog-details/2026-08-08%20-%20theme%20transition%20vt%20suppress.adoc[Detailed notes].

== [2.0.6] - 2026-08-08

Public label: *2.0.6+antora.3* (Antora 3).

* *Fix:* Dark-mode admonitions — TIP/NOTE label cell no longer shares the type-tint wash with `td.content` (synced antora-dark-mode 1.4.1 + `site-visual` overrides). See link:changelog-details/2026-08-08%20-%20admonition%20dark-mode%20label%20wash.adoc[Detailed notes].

== [2.0.5] - 2026-08-01

Public label: *2.0.5+antora.3* (Antora 3).

* *Fix / chore:* Root light↔dark theme transition now lives in vendored antora-dark-mode ≥ 1.4.0 (View Transitions + universal `html.adt-theme-animating` fallback). Removed Valentus per-widget `adt-theme-animating` selector list and click-capture cue from `site-visual.js`. Keeps transparent header paint so dark-mode’s solid `.navbar-menu` fill never flashes. See link:changelog-details/2026-08-01%20-%20root%20theme%20transition%20sync.adoc[Detailed notes — 2026-08-01 root theme transition sync].

== [2.0.4] - 2026-08-01

Public label: *2.0.4+antora.3* (Antora 3).

* *Fix:* Light↔dark theme crossfade — header end-controls strip (GitHub + theme toggle) and search omnibox no longer snap while the navbar fades. Transparent `.navbar-menu` / `.adt-header-actions` paint + `filter` transition on the VCS mark; `adt-theme-animating` covers search compound chrome. Pair with `@antora-supplemental/antora-search-chat` ≥ 0.1.3 for the opaque pill transition. See link:changelog-details/2026-08-01%20-%20theme%20transition%20header%20search.adoc[Detailed notes — 2026-08-01 theme transition header + search].

== [2.0.3] - 2026-07-31

Public label: *2.0.3+antora.3* (Antora 3).

* *Fix:* Condensed text-size control — hide with `!important` at ≥1024px (no longer beside read-width on desktop); scale `.doc` via `zoom` so rem headings change too (`font-size` fallback). See link:changelog-details/2026-07-31%20-%20font-size%20hide%20and%20scale.adoc[Detailed notes — 2026-07-31 font-size hide and scale].

== [2.0.2] - 2026-07-28

Public label: *2.0.2+antora.3* (Antora 3).

* *Docs:* Guide page xref:guide:value-proposition.adoc[Value Proposition & Upstream Fixes] — triage of Default UI / Lunr pain vs Valentus-only chrome, with GitLab trackers, for pitching Valentus as a richer default UI.
* *Fix:* Lunr search results dropdown — left/right edges now match the full compound pill (scope + input). Was `left: 0` on `.adt-search-input-wrap` only, so the panel sat under the input cell (large left gap under the scope, tight right). Package + theme: `right: 0` + `width: 100cqi` (`@antora-supplemental/antora-search-chat` 0.1.2). See link:changelog-details/2026-07-25%20-%20search%20dropdown%20symmetric%20edges.adoc[Detailed notes — 2026-07-25 search dropdown symmetric edges].
* *Fix:* Section permalinks (`.anchor` / § beside headings) — hover hit area covers the gutter beside the title and approaches from outside the heading text (no longer `visibility: hidden` dead zone). Keyboard `:focus` / `:focus-visible` still reveals the link. See link:changelog-details/2026-07-25%20-%20section%20permalink%20hit%20area.adoc[Detailed notes — 2026-07-25 section permalink hit area].
* *Fix / polish:* Search scope closed control — dual-tone *Component* / *Version* + muted current name (Antora `page.component` / `page.componentVersion`); menu options *This component — {title}*. `@antora-supplemental/antora-search-chat` 0.1.2. See link:changelog-details/2026-07-25%20-%20search%20scope%20labels%20collapse.adoc[Detailed notes — 2026-07-25 search scope labels + collapse].
* *Fix / layout:* Navbar search no longer absolute-centers over the brand — flex middle slot; at high zoom / narrow width collapses to a search icon popover (full compound + lunr results). Title ellipsizes instead of overlapping.
* *Fix:* Search input typed text / dual-tone placeholder stay clipped inside `#search-input` / `.adt-search-ph`; wrap stays `overflow: visible` for lunr results.
* *Feature / layout:* Condensed viewports — hide useless read-width control; show centered text-size switcher (small / medium / large) in the tool band. Desktop keeps content-width. See link:changelog-details/2026-07-25%20-%20condensed%20font-size%20switcher.adoc[Detailed notes — 2026-07-25 condensed font-size switcher].
* *Fix / layout:* Condensed (mobile) nav drawer — opaque `--adt-nav-bg` (`var(--adt-elevated)`) below 1024px so the tree no longer paints over transparent page content; desktop stays transparent. See link:changelog-details/2026-07-25%20-%20condensed%20nav%20backdrop.adoc[Detailed notes — 2026-07-25 condensed nav backdrop].
* *Brand:* Replace anatomical heart outline with a solid red blood-drop mark (`logo.svg` / `logo-dark.svg`); demo playbooks use brand strategy (single asset). See link:changelog-details/2026-07-25%20-%20blood-drop%20brand%20mark.adoc[Detailed notes — 2026-07-25 blood-drop brand mark].
* *Polish:* Breadcrumb component/version kickers (`.adt-bc-kicker`) — small muted stroke chevron after the label (same family as nav expanders); flips when `aria-expanded`.
* *Fix:* Search scope caret — defective dual CSS-gradient “triangle” (tongue tip) replaced in standalone `@antora-supplemental/antora-search-chat` with a stroke SVG mask on `.adt-search-scope-slot::after` (reinstall/bump the git dep to pick up).
* *Change:* Consume `@antora-supplemental/antora-search-chat` from https://github.com/antora-supplemental/antora-search-chat[the standalone package] (git dep); remove in-tree `packages/antora-search-chat/` so Valentus is not a second source of truth.
* *Fix:* Lunr search results invisible after compound markup — `@antora/lunr-extension` `search-ui.js` appends `.search-result-dropdown-menu` to `#search-input.parentNode` (`.adt-search-input-wrap`), not to `#search-field`. A prior change set `.adt-search-compound { overflow: visible }` but left `.adt-search-input-wrap { overflow: hidden }` for pill clipping, which still hid all results. Theme override + package: keep compound *and* input-wrap `overflow: visible`; clip only `.adt-search-scope-slot`.
* *Fix / layout:* Sticky footer — `.adt-shell` / `.body.adt-body` fill leftover viewport (`min-height: 100vh` on `body` + flex grow on the shell); `footer.footer` stays natural height outside the 3-col group (no longer grows to absorb slack).
* *Fix / layout:* Footer is full-bleed chrome — no longer `max-width: var(--adt-shell-max)`, so it does not resize with read-width / the 3-column shell.
* *Fix / polish:* Search scope + breadcrumb kickers — drop the double focus ring on click (inset `:focus-visible` stacked on compound `:focus-within` / UA outline); soft accent fill and keyboard-only rings instead.
* *Fix / polish:* Search scope left segment — wrap select in `.adt-search-scope-slot` so hover/focus fill covers the full-height button area (native `<select>` only painted a thin content slice).
* *Fix / polish:* Breadcrumb trail — drop flex `column-gap` that stacked on Antora `li::after` `/` padding, so crumbs sit optically centered on the slash midpoints.
* *Fix / polish:* Breadcrumb home control — same rounded-square kicker chrome (border + fill + 1.5rem) so it aligns with component/version pills.
* *Fix:* Search `/` shortcut — `appendFooterScript` used a shared footer marker, so only `site-search-chat-config.js` was injected and `site-search-chat.js` (keydown handler) was skipped; `site-visual.js` correctly deferred to search-chat and did nothing.
* *Fix:* Header site title (`.adt-header-title`) links via relative `siteRootPath` (current site home), not absolute `site.url`.
* *Fix:* Header brand links — title uses `relativize site.homeUrl`; logo only uses an absolute/external URL when `site.keys.siteHomeUrl` is set (no more silent fallback to playbook `site.url`, which sent local preview to production Pages).
* *Fix / polish:* Search placeholder — Ask off → *Search* + muted *(AI mode not configured)*; Ask on → *Search or Ask*.

== [2.0.1] - 2026-07-25

Public label: *2.0.1+antora.3* (Antora 3).

* *Fix / layout:* Move compound Search or Ask omnibox to the center of `.adt-site-navbar`; keep `adt-read-width` in `.adt-tool-band` over the doc column. Search width is independent of doc-resize / nav-column measure. Affirms that prior “search over nav / resize over doc” in the tool band was inverted for a command-palette search.
* *Fix:* Scope dropdown contrast (`--adt-ink` on `--adt-elevated`, light + dark) and padding/min-width so “This component” is not clipped by the chevron.
* See link:changelog-details/2026-07-25%20-%20search%20navbar%20read-width%20toolband.adoc[Detailed notes — 2026-07-25 search navbar + read-width tool band].

== [2.0.0] - 2026-07-25

Public label: *2.0.0+antora.3* (Antora 3).

*BREAKING* for theme modders (Valentus major): visual redesign and layout chrome contract — tokens, fonts, true 3-column shell, tool-band, nav/TOC styling. Sites with supplemental partial/CSS overlays that assume 1.x structure need a migration pass.

* *Versioning:* Adopt dual-axis labels `VALENTUS_SEMVER+antora.N` so Valentus breaking changes and Antora framework majores stay independent. See xref:guide:version-pinning.adoc[Version Pinning] and `supported-lines.json`.
* *Feature:* Scaffold `@antora-supplemental/antora-search-chat` — wraps `@antora/lunr-extension` (no fork). Product target: one omnibox (*Search or Ask*) + *conjoined* scope control (`[ Scope ▾ | field ]`) in `.adt-tool-band`; lunr-first; optional tier-0 + remote `backend_url` (no API keys). Ask defaults off → struck *or Ask*. Phase 1: compound CSS/markup stub; Ask tabs transitional. See `packages/antora-search-chat/ARCHITECTURE.adoc`.
* *Feature:* Search/Ask omnibox fake placeholder — *Search or Ask*; when Ask is off, *or Ask* is greyed + struck (`.adt-search-ph-ask.is-disabled`). Driven by `ask_enabled` / `backend_url` / `local_assist`.
* See link:changelog-details/2026-07-25%20-%20antora-search-chat%20scaffold.adoc[Detailed notes — 2026-07-25 antora-search-chat scaffold].
* *Fix:* Double-click / triple-click can select text in `#search-input` again when search sits under `.nav-panel-menu`. Antora UI default `site.js` calls `preventDefault` on multi-click `mousedown` in the nav panel (to avoid selecting nav labels); Valentus stops that event at `#search-field`. Also set `user-select: text` on the search input. Tool-band search is outside the panel; search-chat keeps the same guard when present.
* See link:changelog-details/2026-07-25%20-%20search%20dblclick%20select.adoc[Detailed notes — 2026-07-25 search dblclick select].
* *Polish:* Breadcrumb trail weights — ancestors `400`, current crumb `500` (kickers unchanged); hide the vertical `|` between component/version kickers (`.adt-bc-sep`).
* *Demo / breaking chrome:* Visual redesign — teal design tokens; Plus Jakarta Sans + Source Sans 3 + IBM Plex Mono (no serif); true 3-column shell (`nav | main | TOC` peers, gap-only separation, centered shrink-wrap); roomier nav current=hover highlights and text-height stroke chevrons (no left inset bar); dark code panels on `.listingblock > .content`.
* *Polish:* Search + `adt-read-width` live in a full-width `.adt-tool-band` above the 3-col row (not inside nav / `article.doc`); equal `--adt-side-width` for nav/TOC; no muddy hover fill on the search field. Fixes search results clipped by left-column overflow.
* *Change:* Search focus shortcut is plain `/` (skips when typing in inputs); field hint shows `/`. Removed platform modkey FOUC (`data-adt-modkey` / `⌘K` / `Ctrl+K`).
* *Feature:* Standalone `read-width` UI module (`adt-read-width`) — placeable partial; demo sits in the tool band over the article column. Tiers set `--adt-doc-max` only (`68ch` / `82ch` / `100ch`) with constant `--adt-col-gap` (`2rem`); shell shrink-wraps around panels + doc.
* *Gotchas (layout/width):* Avoid shell-max slack (panels “space out”); width transition needs explicit `width`/`max-width` both ways (jumped `flex-basis` snapped on shrink); `scrollbar-gutter: stable` prevents short-page alignment shift; Antora `content.sources` `url: .` fails in a linked worktree — use `antora-playbook-visual-demo.yml`.
* *Fix:* Restore missing AsciiDoc table delimiters (|===) in vcs-repo-logo.adoc so provider and files-involved tables render.
* See link:changelog-details/2026-07-25%20-%20visual%20redesign%20demo.adoc[Detailed notes — 2026-07-25 visual redesign].
* *Fix:* Mouse wheel over the left nav or in-page TOC scrolls the page again. Default UI sets `overscroll-behavior: none` on those panels, which trapped the wheel when the panel had nothing left to scroll; Valentus restores scroll chaining.
* See link:changelog-details/2026-07-25%20-%20sidebar%20wheel%20scroll%20chaining.adoc[Detailed notes — 2026-07-25].
* *Fix:* Move search + read-width outside the 3-col group into `.adt-tool-band` so Lunr results are not clipped by `.nav-panel-menu` overflow.
* See link:changelog-details/2026-07-25%20-%20tool-band-outside-columns.adoc[Detailed notes — 2026-07-25 tool band outside columns].
* See link:changelog-details/2026-07-25%20-%202.0.0%20dual-axis%20versioning.adoc[Detailed notes — 2026-07-25 dual-axis versioning].

== [1.1.2] - 2026-07-24

Public label (retroactive): *1.1.2+antora.3*. Git tag remains `v1.1.2`.

* *Header logo:* Brand-first dark mode — no unconditional invert on `.adt-header-logo`. Colored marks stay as authored; opt in with `site.keys.header_logo_tone: mono` for silhouette marks.
* *Header logo:* Optional light/dark asset pair via `site.keys.header_logo_dark` (CSS swap on `html.dark-theme`). Valentus ships an anatomical heart outline (`logo.svg` / `logo-dark.svg`) and enables the pair on demo playbooks.
* *Fix:* UI partials read camelCased `site.keys` (`headerLogoDark`, `darkModeNavbar`, …). Snake_case lookups never matched Antora's UI model, so playbook branding keys were ignored.
* *Docs:* Guide page `header-logo.adoc` with brand/mono fixture mocks so single-file strategies are documented without a second logo in the live chrome.
* See link:changelog-details/2026-07-24%20-%201.1.2%20brand-first%20header%20logo.adoc[Detailed notes — 2026-07-24].

== [1.1.1] - 2026-06-13

Public label (retroactive): *1.1.1+antora.3*. Git tag remains `v1.1.1`.

* *Dark-mode toggle:* Default `site.keys.dark_mode_navbar` to `light` so the theme toggle uses a dark icon on Valentus' light top bar; vendors `antora-dark-mode` v1.3.1 navbar-tone support.
* *CI:* Fix Release workflow `matches()` guard, dynamic antora-dark-mode pin from `antora-playbook.yml`, `workflow_dispatch` re-publish.

== [1.1.0] - 2026-06-13

Public label (retroactive): *1.1.0+antora.3*. Git tag remains `v1.1.0`.

* *Bundled dark mode:* Release `ui-bundle.zip` merges link:https://github.com/antora-supplemental/antora-dark-mode[antora-dark-mode] with Valentus chrome. Successor install path for former `antora-dark-theme` consumers.
* *Partials:* `head-meta.hbs` and `footer-scripts.hbs` include FOUC prevention, `site-extra.css`, and `site-dark-mode.js`.
* *Maintainers:* `pnpm dark-mode:sync` vendors dark mode CSS/JS from `antora-dark-mode`; release CI runs sync before bundling.

link:changelog-details/2026-06-13%20-%201.1.0%20bundled%20dark%20mode.adoc[Detailed notes — 2026-06-13]

== [1.0.0] - 2026-06-09

Public label (retroactive): *1.0.0+antora.3*. Git tag remains `v1.0.0`.

* Initial release: doc-site chrome split from `antora-dark-theme` into `valentus-theme` (mast, header, layout, VCS icons, UI modules orchestrator).
* Pair with link:https://github.com/antora-supplemental/antora-dark-mode[antora-dark-mode] for dark styling and the theme toggle.
* GitHub Pages demo via `.github/workflows/deploy.yml`.

link:changelog-details/2026-06-09%20-%201.0.0%20initial%20chrome%20split.adoc[Detailed notes — 2026-06-09]
