= 2026-08-08 — Theme transition VT suppress

* *Vendors* antora-dark-mode **1.4.2**: during View Transitions, suppress per-widget CSS paint transitions (`html.adt-theme-vt` + `:active-view-transition`) so Valentus `--adt-transition` (0.2s) chrome / search inputs do not finish ahead of the 0.28s root snapshot crossfade.
* *Kept* hover transitions via `--adt-transition`; they only pause while the theme VT is active.
* Playbook pins point at `antora-dark-mode` `v1.4.2`; release CI re-syncs via `pnpm dark-mode:sync`.
